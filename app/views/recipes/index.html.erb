<h1>Recipes</h1>

<ul>
  <% @recipes.each do |recipe| %>
    <li><%= recipe.name %></li>
  <% end %>
</ul>

<h2>Tags</h2>
<ul>
  <% @tags.each do |tag| %>
    <li> <%= tag.name %></li>
    <% end %>
</ul>
